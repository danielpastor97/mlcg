Data Representation
===================

`mlcg.data` defines the `AtomicData` class, which organizes various system properties and collects the ouputs of models.

.. autoclass:: mlcg.data.atomic_data.AtomicData
