version: 2.0

jobs:
  test:
    docker:
      - image: nec4/pytorch_geometric_v1:latest
    steps:
      - checkout
      - run:
          name: "Run tests/formatting checks"
          command: pip install .;pytest .

workflows:
  version: 2
  build_and_test:
    jobs:
      - test
